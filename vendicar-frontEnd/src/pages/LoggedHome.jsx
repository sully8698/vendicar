import React from "react";
import { getuserinfo } from "../../api/authApi";

export default function LoggedHome({userToken}) {

    // const [user, setUser] = useState([])

    // const createUser = () => {
    //     return user.map( (w,i) => <User key={i} user={w}/>)
    //   }

    return(
        <h1>this is a car....vroom</h1>
       

    )
}